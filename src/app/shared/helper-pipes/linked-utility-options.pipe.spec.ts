import { LinkedUtilityOptionsPipe } from './linked-utility-options.pipe';

describe('LinkedUtilityOptionsPipe', () => {
  it('create an instance', () => {
    const pipe = new LinkedUtilityOptionsPipe();
    expect(pipe).toBeTruthy();
  });
});
