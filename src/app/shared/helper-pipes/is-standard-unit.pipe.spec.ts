import { IsStandardUnitPipe } from './is-standard-unit.pipe';

describe('IsStandardUnitPipe', () => {
  it('create an instance', () => {
    const pipe = new IsStandardUnitPipe();
    expect(pipe).toBeTruthy();
  });
});
