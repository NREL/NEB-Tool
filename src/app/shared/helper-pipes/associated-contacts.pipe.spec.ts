import { AssociatedContactsPipe } from './associated-contacts.pipe';

describe('AssociatedContactsPipe', () => {
  it('create an instance', () => {
    const pipe = new AssociatedContactsPipe();
    expect(pipe).toBeTruthy();
  });
});
