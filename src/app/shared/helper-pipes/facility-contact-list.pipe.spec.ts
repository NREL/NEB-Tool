import { FacilityContactListPipe } from './facility-contact-list.pipe';

describe('FacilityContactListPipe', () => {
  it('create an instance', () => {
    const pipe = new FacilityContactListPipe();
    expect(pipe).toBeTruthy();
  });
});
