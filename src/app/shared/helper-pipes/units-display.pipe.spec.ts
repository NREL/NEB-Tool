import { UnitsDisplayPipe } from './units-display.pipe';

describe('UnitsDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new UnitsDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
