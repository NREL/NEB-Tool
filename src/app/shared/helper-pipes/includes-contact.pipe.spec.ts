import { IncludesContactPipe } from './includes-contact.pipe';

describe('IncludesContactPipe', () => {
  it('create an instance', () => {
    const pipe = new IncludesContactPipe();
    expect(pipe).toBeTruthy();
  });
});
