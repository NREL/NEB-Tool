import { AssessmentListOnSitePipe } from './assessment-list-on-site.pipe';

describe('AssessmentListOnSitePipe', () => {
  it('create an instance', () => {
    const pipe = new AssessmentListOnSitePipe();
    expect(pipe).toBeTruthy();
  });
});
