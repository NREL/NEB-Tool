import { EnergyEquipmentDisplayPipe } from './energy-equipment-display.pipe';

describe('EnergyEquipmentDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new EnergyEquipmentDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
