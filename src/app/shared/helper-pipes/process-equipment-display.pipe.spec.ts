import { ProcessEquipmentDisplayPipe } from './process-equipment-display.pipe';

describe('ProcessEquipmentDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new ProcessEquipmentDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
