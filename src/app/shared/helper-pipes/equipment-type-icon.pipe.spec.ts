import { EquipmentTypeIconPipe } from './equipment-type-icon.pipe';

describe('EquipmentTypeIconPipe', () => {
  it('create an instance', () => {
    const pipe = new EquipmentTypeIconPipe();
    expect(pipe).toBeTruthy();
  });
});
