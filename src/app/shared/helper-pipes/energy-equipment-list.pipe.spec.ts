import { EnergyEquipmentListPipe } from './energy-equipment-list.pipe';

describe('EnergyEquipmentListPipe', () => {
  it('create an instance', () => {
    const pipe = new EnergyEquipmentListPipe();
    expect(pipe).toBeTruthy();
  });
});
