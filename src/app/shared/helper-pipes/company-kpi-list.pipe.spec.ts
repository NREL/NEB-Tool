import { CompanyKpiListPipe } from './company-kpi-list.pipe';

describe('CompanyKpiListPipe', () => {
  it('create an instance', () => {
    const pipe = new CompanyKpiListPipe();
    expect(pipe).toBeTruthy();
  });
});
