import { EnergyOpportunityDisplayPipe } from './energy-opportunity-display.pipe';

describe('EnergyOpportunityDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new EnergyOpportunityDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
