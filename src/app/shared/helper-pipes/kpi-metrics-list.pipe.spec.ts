import { KpiMetricsListPipe } from './kpi-metrics-list.pipe';

describe('KpiMetricsListPipe', () => {
  it('create an instance', () => {
    const pipe = new KpiMetricsListPipe();
    expect(pipe).toBeTruthy();
  });
});
