import { LinkedUnitOptionsPipe } from './linked-unit-options.pipe';

describe('LinkedUnitOptionsPipe', () => {
  it('create an instance', () => {
    const pipe = new LinkedUnitOptionsPipe();
    expect(pipe).toBeTruthy();
  });
});
