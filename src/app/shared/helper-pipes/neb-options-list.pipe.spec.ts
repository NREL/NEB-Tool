import { NebOptionsListPipe } from './neb-options-list.pipe';

describe('NebOptionsListPipe', () => {
  it('create an instance', () => {
    const pipe = new NebOptionsListPipe();
    expect(pipe).toBeTruthy();
  });
});
