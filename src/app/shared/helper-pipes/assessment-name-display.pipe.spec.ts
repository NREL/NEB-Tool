import { AssessmentNameDisplayPipe } from './assessment-name-display.pipe';

describe('AssessmentNameDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new AssessmentNameDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
