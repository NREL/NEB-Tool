import { AddressDisplayPipe } from './address-display.pipe';

describe('AddressDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new AddressDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
