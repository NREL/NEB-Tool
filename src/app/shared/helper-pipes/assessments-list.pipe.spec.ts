import { AssessmentsListPipe } from './assessments-list.pipe';

describe('AssessmentsListPipe', () => {
  it('create an instance', () => {
    const pipe = new AssessmentsListPipe();
    expect(pipe).toBeTruthy();
  });
});
