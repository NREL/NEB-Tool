import { ProjectDisplayPipe } from './project-display.pipe';

describe('ProjectDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
