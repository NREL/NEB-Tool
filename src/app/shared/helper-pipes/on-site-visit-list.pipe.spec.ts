import { OnSiteVisitListPipe } from './on-site-visit-list.pipe';

describe('OnSiteVisitListPipe', () => {
  it('create an instance', () => {
    const pipe = new OnSiteVisitListPipe();
    expect(pipe).toBeTruthy();
  });
});
