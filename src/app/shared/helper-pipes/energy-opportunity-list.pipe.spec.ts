import { EnergyOpportunityListPipe } from './energy-opportunity-list.pipe';

describe('EnergyOpportunityListPipe', () => {
  it('create an instance', () => {
    const pipe = new EnergyOpportunityListPipe();
    expect(pipe).toBeTruthy();
  });
});
