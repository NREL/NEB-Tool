import { KpiCategoryClassPipe } from './kpi-category-class.pipe';

describe('KpiCategoryClassPipe', () => {
  it('create an instance', () => {
    const pipe = new KpiCategoryClassPipe();
    expect(pipe).toBeTruthy();
  });
});
