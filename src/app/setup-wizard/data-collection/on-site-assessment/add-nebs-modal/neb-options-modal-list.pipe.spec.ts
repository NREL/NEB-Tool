import { NebOptionsModalListPipe } from './neb-options-modal-list.pipe';

describe('NebOptionsModalListPipe', () => {
  it('create an instance', () => {
    const pipe = new NebOptionsModalListPipe();
    expect(pipe).toBeTruthy();
  });
});
