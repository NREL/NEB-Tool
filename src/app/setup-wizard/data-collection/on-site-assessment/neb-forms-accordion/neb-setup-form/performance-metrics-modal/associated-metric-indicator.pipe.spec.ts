import { AssociatedMetricIndicatorPipe } from './associated-metric-indicator.pipe';

describe('AssociatedMetricIndicatorPipe', () => {
  it('create an instance', () => {
    const pipe = new AssociatedMetricIndicatorPipe();
    expect(pipe).toBeTruthy();
  });
});
