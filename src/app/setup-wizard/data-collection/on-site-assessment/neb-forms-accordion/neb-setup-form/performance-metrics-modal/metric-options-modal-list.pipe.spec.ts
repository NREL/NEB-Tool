import { MetricOptionsModalListPipe } from './metric-options-modal-list.pipe';

describe('MetricOptionsModalListPipe', () => {
  it('create an instance', () => {
    const pipe = new MetricOptionsModalListPipe();
    expect(pipe).toBeTruthy();
  });
});
