import { KpiUnitOptionPipe } from './kpi-unit-option.pipe';

describe('KpiUnitOptionPipe', () => {
  it('create an instance', () => {
    const pipe = new KpiUnitOptionPipe();
    expect(pipe).toBeTruthy();
  });
});
