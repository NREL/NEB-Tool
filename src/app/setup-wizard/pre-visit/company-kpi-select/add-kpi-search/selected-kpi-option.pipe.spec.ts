import { SelectedKpiOptionPipe } from './selected-kpi-option.pipe';

describe('SelectedKpiOptionPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedKpiOptionPipe();
    expect(pipe).toBeTruthy();
  });
});
